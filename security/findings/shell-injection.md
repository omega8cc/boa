# Findings: Shell Injection (General)

Covers dynamic command construction from unvalidated input, unsafe wget/curl URL building, and pipe-chain injection.

Findings are appended below as they are discovered. Each entry follows the schema defined
in CLAUDE.md. Most-recent findings appear at the bottom.

---

