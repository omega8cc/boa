#%PAM-1.0

# PAM configuration for Pure-FTPd, deployed by Barracuda to
# /etc/pam.d/pure-ftpd. Upstream's sample uses pam_stack.so, which does
# not exist in Linux-PAM; this is the pam_unix form BOA has always
# produced on the box (see _fix_ftps_pam).

auth       required     pam_listfile.so item=user sense=deny file=/etc/ftpusers onerr=succeed
auth       required     pam_shells.so
auth       required     pam_nologin.so

account    required     pam_unix.so

password   required     pam_unix.so

session    required     pam_unix.so
